import 'package:flutter/material.dart';

class Historyscree extends StatelessWidget {
  const Historyscree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Historyscreen"),)
    );
  }
}